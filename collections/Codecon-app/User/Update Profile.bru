meta {
  name: Update Profile
  type: http
  seq: 2
}

put {
  url: http://localhost:8080/api/users/profile
  body: json
  auth: inherit
}

body:json {
  {
    "firstName": "Rica",
    "bio": "Full stack developer"
  }
}
